{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>BasePrice</th>
                <td>{{ order.basePrice }}</td>
            </tr>
            <tr>
                <th>AdditionalPrice</th>
                <td>{{ order.additionalPrice }}</td>
            </tr>
            <tr>
                <th>DriversRating</th>
                <td>{{ order.driversRating }}</td>
            </tr>
            <tr>
                <th>PasangerRating</th>
                <td>{{ order.pasangerRating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_index') }}">back to list</a>

    <a href="{{ path('order_edit', {'id': order.id}) }}">edit</a>

    {{ include('order/_delete_form.html.twig') }}
{% endblock %}
