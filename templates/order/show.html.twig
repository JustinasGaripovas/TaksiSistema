{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>BasePrice</th>
                <td>{{ order.basePrice }}</td>
            </tr>
            <tr>
                <th>AdditionalPrice</th>
                <td>{{ order.additionalPrice }}</td>
            </tr>
            <tr>
                <th>DriverRating</th>
                <td>{{ order.driverRating }}</td>
            </tr>
            <tr>
                <th>PassengerRating</th>
                <td>{{ order.passengerRating }}</td>
            </tr>
            <tr>
                <th>LatCoordinates</th>
                <td>{{ order.latCoordinate }}</td>
            </tr>
            <tr>
                <th>LngCoordinates</th>
                <td>{{ order.lngCoordinate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_index') }}">back to list</a>

    <a href="{{ path('order_edit', {'id': order.id}) }}">edit</a>

    {{ include('order/_delete_form.html.twig') }}
{% endblock %}
