{% extends 'base.html.twig' %}

{% block title %}Driver{% endblock %}

{% block body %}
    <h1>Driver</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ driver.id }}</td>
            </tr>
            <tr>
                <th>DriverId</th>
                <td>{{ driver.driverId }}</td>
            </tr>
            <tr>
                <th>AverageRating</th>
                <td>{{ driver.averageRating }}</td>
            </tr>
            <tr>
                <th>CompleteOrderCount</th>
                <td>{{ driver.completeOrderCount }}</td>
            </tr>
            <tr>
                <th>UnfinishedOrderCount</th>
                <td>{{ driver.unfinishedOrderCount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('driver_index') }}">back to list</a>

    <a href="{{ path('driver_edit', {'id': driver.id}) }}">edit</a>

    {{ include('driver/_delete_form.html.twig') }}
{% endblock %}
