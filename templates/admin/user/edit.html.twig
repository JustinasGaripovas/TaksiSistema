{% extends 'admin/base.html.twig' %}

{% block title %}Admin - User {{ user }} edit{% endblock %}

{% block body_title %}<h1>Edit User {{ user }}</h1>{% endblock %}

{% block body_content %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_user_index') }}">Back</a>
{% endblock %}
