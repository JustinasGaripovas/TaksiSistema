{% extends 'admin/base.html.twig' %}

{% block title %}Admin - Discount {{ discount.name }} edit{% endblock %}

{% block body_content %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}


    <a href="{{ path('admin_vehicle_type_index') }}">Back</a>

{% endblock %}
