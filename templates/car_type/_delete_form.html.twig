<form method="post" action="{{ path('car_type_delete', {'id': car_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_type.id) }}">
    <button class="btn">Delete</button>
</form>
